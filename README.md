# Minecraft Server

Este repositório versiona a base do servidor e do modpack:

- `mods/`
- `config/`
- `defaultconfigs/`
- scripts de inicialização
- arquivos principais de configuração do servidor

Arquivos gerados em runtime, mundo salvo, logs e estado administrativo local ficam fora do Git via `.gitignore`.
